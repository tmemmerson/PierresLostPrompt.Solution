@{
  Layout = "_Layout";
}

@using Bakery.Models;

<h1>Vendor List</h1>

@if (Model.Count == 0)
{
  <h3>You have no vendor logs currently.</h3>
}

@foreach (Vendor vendor in Model)
{
  <h3><a href="/vendors/@vendor.Id">@vendor.Name</a></h3>
}

<p><a href="/">Home</a></p>